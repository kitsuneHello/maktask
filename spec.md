# MakTask アプリケーション仕様書

## 1. アプリケーション概要

**MakTask**は、プロジェクト管理およびタスク管理を行うデスクトップ/ウェブアプリケーションである。
ユーザーはタブ形式のUIを通じて、複数のプロジェクトやタスク詳細を並行して閲覧・編集できる。

## 2. UIアーキテクチャ

* **メイン構造**: アプリケーション全体は「タブビュー (Tab View)」で構成される。
* **初期状態**: アプリ起動時に「ホームタブ」が自動的に開く。
* **タブの挙動**: プロジェクト詳細やタスク詳細は、ポップアップや画面遷移ではなく、メインのタブバーに「新しいタブ」として追加される。

## 3. データモデル要件

* **Project (プロジェクト)**: ID, 名前, テーマカラー, 表示状態(Boolean: タスクビューに表示するか否か)
* **Task (タスク)**: ID, プロジェクトID, タスク名, 開始日時, 終了日時, 期限日(Optional), 終日フラグ(Boolean), 期限モードフラグ(Boolean), メモ(Markdown), サブタスクリスト
* **Subtask (サブタスク)**: ID, タスクID, サブタスク名, 開始日時, 終了日時, 完了状態(Boolean)
* **ProgressLog (進捗記録)**: ID, タスクID, タイトル, 記録日時, メモ

## 4. 機能詳細：ホームタブ (Home Tab)

ホームタブは、左側の「サイドバー（プロジェクトビュー）」と、中央〜右側の「メインエリア（タスクビュー）」で構成される。

### A. サイドバー：プロジェクトビュー (Project View)

* **プロジェクト一覧表示**: 作成済みのプロジェクトをリスト表示する。
* **表示切り替えスイッチ**: 各プロジェクト名の左横にスライドボタン（Toggle Switch）を配置。
  * `ON`: メインエリア（タスクビュー）に対象プロジェクトのタスクを表示する。
  * `OFF`: 非表示にする。
  * *複数選択可能*。

* **新規作成**: 「プロジェクト新規作成」ボタンを配置。クリックすると**新しいタブ**で「プロジェクト作成ページ」が開く。
* **詳細表示**: プロジェクト名をクリックすると、**新しいタブ**で「プロジェクト詳細ページ」が開く。
* **コンテキストメニュー (右クリック)**:
  * プロジェクト項目を右クリックでメニューを表示。
  * `編集`: プロジェクト情報を編集。
  * `削除`: プロジェクトを削除。

### B. メインエリア：タスクビュー (Task View)

サイドバーで「表示(ON)」に設定されたプロジェクトに紐づくタスクのみを表示する。
上部のボタンで以下の3つのビューを切り替え可能。

#### 1. カレンダービュー (Calendar View)

* **表示形式**: 月表示カレンダー。
* **タスクの描画**:
  * 長方形のバーとして表示し、バーの中にタスク名を記載する。
  * **複数日またぎ**: 連続したバーとして表示する（週替わりでの改行は許容）。
  * **重なり順**: 期間が長いタスクを優先的に上に配置し、縦に積み重ねる。
  * **オーバーフロー処理**: 1日のマスの高さに収まりきらない場合、全てを表示せず「...」等のインジケータを表示し、省略されていることを明示する。

* **インタラクション**:
  * **左クリック (タスクバー)**: **新しいタブ**で「タスク詳細ページ」を開く。
  * **右クリック (日付マス)**: コンテキストメニューを表示。
    * `その日のタスク一覧`: リスト形式で表示。クリックでタスク詳細タブを開く。
    * `この日にタスクを作成`: 新規タスク作成タブを開く（初期値設定あり、後述）。

#### 2. ガントチャートビュー (Gantt Chart View)

* **軸構成**:
  * 縦軸: タスクリスト
  * 横軸: 時間（Time）

* **表示単位切り替え**: ボタン等で「月」「週」「日」のスケール切り替えが可能。
* **スクロール**: 横スクロールで過去・未来へ移動可能。
* **インタラクション**:
  * **左クリック (タスクバー)**: **新しいタブ**で「タスク詳細ページ」を開く。
  * **右クリック (タスクバー)**: コンテキストメニューに「削除」を表示。

#### 3. リストビュー (List View)

* **グルーピング**: 選択されているプロジェクトごとにグループ化して表示する。
* **ソート順**: 各プロジェクト内で、開始日（または期限）が早い順にソートする。
* **インタラクション**:
  * **左クリック (タスク名)**: **新しいタブ**で「タスク詳細ページ」を開く。
  * **右クリック (タスク行)**:
    * `削除`: タスクを削除。
    * `このプロジェクトにタスクを追加する`: 新規タスク作成タブを開く（初期値設定あり、後述）。

## 5. 機能詳細：作成・編集フロー

### A. プロジェクト作成ページ (新しいタブ)

* **入力項目**:
  * プロジェクト名 (Text)
  * テーマカラー (Color Picker)

* **アクション**: 作成ボタン押下で保存し、タブを閉じる。

### B. タスク作成ページ (新しいタブ)

* **起動エントリポイントと初期値**:
  1. **「タスク作成」ボタン (共通)**: プロジェクト未選択、日付＝今日。
  2. **カレンダー/ガントチャートの右クリック「この日にタスクを作成」**: プロジェクト未選択、開始日＝選択した日付。
  3. **リストの右クリック「このプロジェクトにタスクを追加」**: プロジェクト＝選択したプロジェクト、日付＝今日。

* **入力フォーム**:
  * **プロジェクト選択**: ドロップダウンリスト（エントリポイントにより初期値が入る）。
  * **タスク名**: テキスト入力。
  * **期間設定**:
    * `開始日時`: デフォルトはエントリポイント依存または今日。
    * `終了日時`: デフォルトは開始日と同じ。
    * `終日 (Toggle)`: ONの場合、時刻入力を無効化し日付のみにする。
    * `期限 (Toggle)`: ONの場合、期間（Range）ではなく「期限日（Deadline）」としての扱いに切り替える。

  * **サブタスク**:
    * 複数のサブタスクを追加可能。
    * 各サブタスクに「名前」と「期間（親タスクと同様の仕様）」を設定可能。

  * **メモ**:
    * Markdown記法をサポート。
    * エディタと並んでリアルタイムプレビューを表示する。

* **完了アクション**: 保存後、作成タブを閉じ、自動的に作成されたタスクの「タスク詳細タブ」を開く。

### C. タスク詳細ページ (新しいタブ)

* **表示内容**: タスクの全パラメータを表示。
* **サブタスク**: Todoリスト形式（チェックボックス付き）で表示し、完了/未完了を切り替え可能。
* **編集**: 「編集」ボタンで各パラメータを変更モードにする。
* **進捗記録 (Progress Log)**:
  * **入力**: タイトル、日時、メモを入力して追加。
  * **表示**: タスク詳細タブ内に、このタスク専用の「カレンダー形式」または「リスト形式」でログ履歴を表示する。

---

*End of Specification*
