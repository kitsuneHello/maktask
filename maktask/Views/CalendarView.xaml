<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="maktask.Views.CalendarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:maktask.Views"
    xmlns:converters="using:maktask.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:ColorStringToBrushConverter x:Key="ColorToBrushConverter"/>
    </UserControl.Resources>

    <Grid x:Name="CalendarGrid" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Day Headers -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="日" HorizontalAlignment="Center" Foreground="Red"/>
            <TextBlock Grid.Column="1" Text="月" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="2" Text="火" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="3" Text="水" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="4" Text="木" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="5" Text="金" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="6" Text="土" HorizontalAlignment="Center" Foreground="Blue"/>
        </Grid>

        <!-- Calendar Days Grid -->
        <Grid x:Name="DaysGrid" Grid.Row="1">
        </Grid>
    </Grid>
</UserControl>
